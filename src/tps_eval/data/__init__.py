"""tps_eval shared data-loading helpers (sequences, embeddings, results).

Imported qualified as ``tps_eval.data`` — e.g. ``from tps_eval.data.sequences
import ...`` — via the installed ``tps_eval`` package (``pip install -e .``), so
it never collides with a top-level ``data`` package that some satellite conda
env (e.g. an ESMFold/ProteinMPNN env) might ship.
"""
